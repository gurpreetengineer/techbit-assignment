/**
 * We will check if the input box is email or normal box.
 * if it's normal input, then there would be a limit of at least 2 input characters.
 * Also, there would be type of input which will be managed here. 
 * 
 * We shall be using 'type' props to manage it.
 */
